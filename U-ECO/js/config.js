/* =========================================================
   Kakao Map 설정
   =========================================================
   1. 아래 KAKAO_APP_KEY 자리에 카카오 개발자 콘솔에서 발급받은
      "JavaScript 키"를 넣으세요. (REST API 키 아님! JavaScript 키!)
      https://developers.kakao.com/console/app → 내 애플리케이션 → 앱 키

   2. 같은 콘솔의 "플랫폼" 설정에서 Web 플랫폼에
      실제 배포 주소(GitHub Pages 도메인)를 등록해야 지도가 뜹니다.
      예: https://yeonsu6531.github.io
      (등록 안 하면 콘솔에 아무 에러도 안 뜨고 그냥 지도가 하얗게 나와요)

   3. CAMPUS_CENTER는 지도를 처음 열었을 때 보이는 중심 좌표입니다.
      울산대학교 대략 중심으로 맞춰뒀는데, 필요하면 미세 조정하세요.
========================================================= */

const KAKAO_APP_KEY = "dcd6ec2004bcedcb0ecdc49d949485ec";

const CAMPUS_CENTER = { lat: 35.5447, lng: 129.2564 }; // 울산대학교 대략 중심
const CAMPUS_LEVEL = 3; // 숫자가 작을수록 확대됨 (1~14)

// 지도를 이 범위 밖으로 못 벗어나게 막고 싶을 때 쓰는 사각형 경계
// (남서쪽 좌표, 북동쪽 좌표) — 필요 없으면 이 기능은 안 써도 됨
const CAMPUS_BOUNDS = {
  sw: { lat: 35.5390, lng: 129.2480 },
  ne: { lat: 35.5510, lng: 129.2650 },
};
